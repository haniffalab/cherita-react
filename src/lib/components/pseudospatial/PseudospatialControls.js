import React from "react";

import { useDataset } from "../../context/DatasetContext";

export function PseudospatialControls() {
  const dataset = useDataset();

  // @TODO: make slider its own component and import it here
  return <></>;
}
